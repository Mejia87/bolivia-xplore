/*import React from 'react';
import {
    Text,
    TouchableOpacity,
    StyleSheet,
    StyleProp,
    ViewStyle,
    TextStyle,
  } from 'react-native';
  import Ionicons from 'react-native-vector-icons/Ionicons';
  
  export default function Button({ label="  Ir a Ubicacion", onPress, containerStyles, textStyles }) {
    return (
      <TouchableOpacity
        onPress={onPress}
        style={[styles.container, containerStyles]}
        activeOpacity={0.8}
      >
        <Text style={[styles.text, textStyles]}>{label}</Text>
      </TouchableOpacity>
    );
  }
  
  const styles = StyleSheet.create({
    text: {
      color: '#f5f5f5',
    },
    container: {
      backgroundColor: '#0071ff',
      height: 30,
      paddingHorizontal: 0,
      flexDirection: 'row',
      borderRadius: 6,
      alignItems: 'center',
      position: 'absolute',
      top: 790,
      left: 200,
      right: 200,
    },
  });
  */